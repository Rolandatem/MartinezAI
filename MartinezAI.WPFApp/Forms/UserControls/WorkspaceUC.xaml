<UserControl x:Class="MartinezAI.WPFApp.Forms.UserControls.WorkspaceUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:MartinezAI.WPFApp.ViewModels.UserControls"
             xmlns:converters="clr-namespace:MartinezAI.WPFApp.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:WorkspaceUCViewModelDesign, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/WorkspaceUCStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <converters:BoolToVisibilityConverter x:Key="boolToVisibilityConverter" />
            <converters:ViewModelToViewConverter x:Key="viewModelToViewConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="300"/>
            <ColumnDefinition Width="5"/>
            <!-- For Splitter -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- TOP MENU -->
        <Menu Grid.Row="0" 
              Grid.Column="0" 
              Grid.ColumnSpan="3" 
              Style="{DynamicResource CustomMenuStyle}">
            <MenuItem Header="Edit Users"
                      Command="{Binding OnEditUsersCommand}"
                      Visibility="{Binding CurrentUser.IsAdmin, Converter={StaticResource boolToVisibilityConverter}}"/>
            <MenuItem Header="Change Password" Command="{Binding OnChangePasswordCommand}" />
            <MenuItem Header="Logout" Command="{Binding DataContext.OnLogoutCommand,
                RelativeSource={RelativeSource AncestorType=Window}}" />
            <MenuItem Header="Exit" Command="{Binding DataContext.OnCloseApplicationCommand,
                RelativeSource={RelativeSource AncestorType=Window}}" />
        </Menu>

        <!-- LEFT COLUMN -->
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header label -->
            <TextBlock Text="Assistants"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="16 16 16 10"
                       Foreground="{DynamicResource SecondaryFontBrush}"/>

            <!-- List of Assistants -->
            <Border Grid.Row="1" Margin="10 0 10 0" Background="{DynamicResource PanelBackgroundBrush}" CornerRadius="8">
                <ListBox ItemsSource="{Binding Assistants}"
                         SelectedItem="{Binding SelectedAssistant, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemContainerStyle="{StaticResource AssistantListItemStyle}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Height="30">
                                <!-- Assistant Name -->
                                <TextBlock Text="{Binding Name}" 
                                           VerticalAlignment="Center"
                                           FontSize="15"
                                           Margin="10,0,0,0"/>
                                <!-- Spacer -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right" Margin="5,0,0,0">
                                    <!-- Edit (Pencil) -->
                                    <Button Style="{StaticResource IconButtonStyle}"
                                            ToolTip="Edit"
                                            Command="{Binding DataContext.OnEditAssistantCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}">
                                        <!-- Unicode pencil as fallback if DrawingImage isn't bound -->
                                        <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="15" Text=""/>
                                        <!-- Unicode for pencil icon -->
                                    </Button>
                                    <!-- Delete (X) -->
                                    <Button Style="{StaticResource IconButtonStyle}"
                                            ToolTip="Delete"
                                            Command="{Binding DataContext.OnDeleteAssistantCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Margin="5,0,5,0">
                                        <TextBlock FontSize="15" Text="✖"/>
                                    </Button>
                                </StackPanel>
                            </DockPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Button at bottom -->
            <Button Grid.Row="2"
                    Content="Create New Assistant"
                    Style="{DynamicResource RoundedButtonStyle}"
                    Margin="16 16 16 16"
                    Command="{Binding OnCreateNewAssistantCommand, Mode=OneWay}"/>
        </Grid>

        <!-- Splitter -->
        <GridSplitter Grid.Row="1" Grid.Column="1"
                      Width="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource SurfaceBrush}"
                      ShowsPreview="True"/>

        <!-- RIGHT COLUMN -->
        <Border Grid.Row="1" Grid.Column="2"
                Margin="10"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="8">
            <ContentControl Content="{Binding WorkspaceContent, Converter={StaticResource viewModelToViewConverter}}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"/>
        </Border>
    </Grid>
</UserControl>