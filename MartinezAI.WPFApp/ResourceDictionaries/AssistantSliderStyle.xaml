<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="AssistantSliderStyle" TargetType="Slider">
        <Setter Property="Minimum" Value="0"/>
        <Setter Property="Maximum" Value="2"/>
        <Setter Property="TickFrequency" Value="0.1"/>
        <Setter Property="IsSnapToTickEnabled" Value="True"/>
        <Setter Property="Height" Value="28"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid Margin="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!-- Minimum track -->
                        <Rectangle x:Name="PART_SelectionRange" Height="6" RadiusX="3" RadiusY="3"
                        Fill="{DynamicResource PrimaryAccentBrush}" VerticalAlignment="Center" Grid.Column="1"/>
                        <!-- Track background -->
                        <Rectangle x:Name="TrackBackground" Height="6" RadiusX="3" RadiusY="3"
                        Fill="{DynamicResource AccentLightBrush}" VerticalAlignment="Center" Grid.Column="1"/>
                        <Track x:Name="PART_Track" Grid.Column="1"
                        IsDirectionReversed="{TemplateBinding IsDirectionReversed}"
                        Maximum="{TemplateBinding Maximum}" Minimum="{TemplateBinding Minimum}"
                        Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        >
                            <Track.Thumb>
                                <Thumb x:Name="Thumb"
                                   Width="20" Height="20"
                                   Background="{DynamicResource PrimaryAccentBrush}"
                                   BorderBrush="{DynamicResource AccentDarkBrush}"
                                   BorderThickness="1"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>