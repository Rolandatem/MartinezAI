<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- BRUSHES -->
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="#FF101218" />
    <SolidColorBrush x:Key="ContainerBackgroundBrush" Color="#FF1F222B" />
    <SolidColorBrush x:Key="ContainerHeaderBrush" Color="#FF1F2D41" />
    <SolidColorBrush x:Key="ContainerBorderBrush" Color="#FF455F87" />
    <SolidColorBrush x:Key="MainFontForegroundBrush" Color="#FFEEF0FF" />
    <SolidColorBrush x:Key="InputForegroundBrush" Color="#FF3F4454" />
    
    <!-- CONTROL -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="FontFamily" Value="Arial" />
    </Style>

    <!-- TEXTBOX -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{StaticResource InputForegroundBrush}" />
        <Setter Property="Height" Value="20" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBoxBase}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{DynamicResource ContainerBorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Focusable="False"
                                      HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Hidden"
                                      Margin="0,2,0,0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" TargetName="border" Value="0.56" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>